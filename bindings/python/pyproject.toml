[project]
name = "injector"
version = "1.0.0"
description = "Python bindings for libinjector (ptrace-based shared library injection)"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
